export interface ChatItem {
    text: string;
    audioSrc?: string;
    isUser: boolean;
  }
  
  

export type TSomething = {
  ifYouAreBored: true;
  andYouJustWanna: "Add a type to avoid adding any"
};
